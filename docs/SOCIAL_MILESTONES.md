# Social milestone drafts

**Do not publish without explicit user approval.** Replace placeholders with genuine links, screenshots, and measured results.

## Milestone 1 — failure reproduced

> EvidenceForge just reproduced a real GitHub Actions failure at the exact commit inside a TrueForge Daytona sandbox. The key difference: the reproduction command and matching failure signature are stored as evidence—the model's summary is not enough. [clip/link]

Required proof: real run ID, commit SHA, Daytona command event, stable signature.

## Milestone 2 — competing hypotheses disproved

> Three read-only TrueForge specialists investigated the same incident in parallel. Missing secret: refuted. Dependency regression: refuted. Validation-order bug: supported. Parallel investigation, serialized patching, evidence-linked hypotheses. [screenshot/link]

Required proof: three subagent traces and evidence-linked ledger.

## Milestone 3 — premature completion refused

> The reviewer said PASS. A deterministic verifier still failed. EvidenceForge refused to complete. Agents shouldn't grade their own homework. [clip/link]

Required proof: reviewer verdict, failed verifier, blocked CompletionGate result.

## Milestone 4 — approval stopped the write

> EvidenceForge prepared a GitHub PR, showed the exact repository/base/head/title/body, and paused. Reject produced BLOCKED; no external write occurred. Approve resumed through TrueForge and then reconciled GitHub state. [clip/link]

Required proof: approval card, denial trace or approved write, reconciliation event.

## Milestone 5 — false-success evaluation

> EvidenceForge fixture comparison: 15 same-input cases. The unenforced baseline falsely completed 8 incomplete/unsafe cases (false-success rate 0.5714); EvidenceForge completed all 6 resolvable cases and falsely completed none. Deterministic control-policy result, not a live model benchmark. [report/link]

Required proof: committed evaluation report and command output. Describe it as a fixture result, not general performance.
